# QLever access token — create out-of-band, NOT managed by kustomize:
#
#   oc create secret generic qlever-credentials \
#     --from-literal=access-token="$(openssl rand -hex 32)" \
#     -n packagegraph
#
# This file is documentation only. It is NOT included in kustomization.yaml
# to avoid overwriting the real token with an empty placeholder.
