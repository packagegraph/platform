apiVersion: v1
kind: Service
metadata:
  name: sparql-proxy
  namespace: packagegraph
  labels:
    app: sparql-proxy
spec:
  type: NodePort
  selector:
    app: sparql-proxy
  ports:
    - port: 8443
      targetPort: 8443
      nodePort: 30443
      name: https
