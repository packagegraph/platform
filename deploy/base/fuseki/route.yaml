apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: fuseki
  namespace: packagegraph
  labels:
    app: fuseki
spec:
  to:
    kind: Service
    name: sparql-proxy
  port:
    targetPort: https
  tls:
    termination: passthrough
