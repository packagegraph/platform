apiVersion: v1
kind: ConfigMap
metadata:
  name: fuseki-config
  namespace: packagegraph
data:
  config.ttl: |
    @prefix fuseki:  <http://jena.apache.org/fuseki#> .
    @prefix rdf:     <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
    @prefix ja:      <http://jena.hpl.hp.com/2005/11/Assembler#> .
    @prefix tdb2:    <http://jena.apache.org/2016/tdb#> .

    <#service> rdf:type fuseki:Service ;
        fuseki:name "packagegraph" ;
        fuseki:endpoint [ fuseki:operation fuseki:query ; fuseki:name "sparql" ] ;
        fuseki:endpoint [ fuseki:operation fuseki:query ; fuseki:name "query" ] ;
        fuseki:endpoint [ fuseki:operation fuseki:update ; fuseki:name "update" ] ;
        fuseki:endpoint [ fuseki:operation fuseki:gsp-r ; fuseki:name "get" ] ;
        fuseki:dataset <#dataset> .

    # NOTE: unionDefaultGraph should be enabled when all data is in named graphs
    # (via pg-collect load). Disabled while data is still loaded into default graph
    # via tdb2.tdbloader. Enable with: tdb2:unionDefaultGraph true
    <#dataset> rdf:type tdb2:DatasetTDB2 ;
        tdb2:location "/data/tdb2" .
