# QLever admin access token, read by qlever.container via -a/--access-token.
# Placeholder value — replace before starting qlever.service:
#   printf 'QLEVER_ACCESS_TOKEN=%s\n' "$(openssl rand -hex 32)" > qlever.env
#   chmod 600 qlever.env && chown root:root qlever.env
QLEVER_ACCESS_TOKEN=CHANGE_ME
