# Schedules pg-collect@ubuntu-noble-riscv64.service (deploy/quadlet/collectors/pg-collect@.container,
# instantiated with %i=ubuntu-noble-riscv64, script: scripts/collectors/ubuntu-noble-riscv64.sh).
# Plain systemd timer, not a quadlet unit -- Podman Quadlet has no .timer
# generator (see qlever-rebuild-index.timer's comment) -- installed as-is
# under /etc/systemd/system/.

[Unit]
Description=Schedule for pg-collect@ubuntu-noble-riscv64.service

[Timer]
OnCalendar=Mon *-*-* 09:00:00
RandomizedDelaySec=120
Persistent=true
Unit=pg-collect@ubuntu-noble-riscv64.service

[Install]
WantedBy=timers.target
