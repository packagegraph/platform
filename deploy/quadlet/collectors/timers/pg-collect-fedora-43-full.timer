# Schedules pg-collect@fedora-43-full.service (deploy/quadlet/collectors/pg-collect@.container,
# instantiated with %i=fedora-43-full, script: scripts/collectors/fedora-43-full.sh).
# Plain systemd timer, not a quadlet unit -- Podman Quadlet has no .timer
# generator (see qlever-rebuild-index.timer's comment) -- installed as-is
# under /etc/systemd/system/.

[Unit]
Description=Schedule for pg-collect@fedora-43-full.service

[Timer]
OnCalendar=Fri *-*-* 02:00:00
RandomizedDelaySec=120
Persistent=true
Unit=pg-collect@fedora-43-full.service

[Install]
WantedBy=timers.target
