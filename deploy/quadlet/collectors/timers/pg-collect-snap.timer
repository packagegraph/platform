# Schedules pg-collect@snap.service (deploy/quadlet/collectors/pg-collect@.container,
# instantiated with %i=snap, script: scripts/collectors/snap.sh).
# Plain systemd timer, not a quadlet unit -- Podman Quadlet has no .timer
# generator (see qlever-rebuild-index.timer's comment) -- installed as-is
# under /etc/systemd/system/.

[Unit]
Description=Schedule for pg-collect@snap.service

[Timer]
OnCalendar=Mon *-*-* 13:00:00
RandomizedDelaySec=120
Persistent=true
Unit=pg-collect@snap.service

[Install]
WantedBy=timers.target
