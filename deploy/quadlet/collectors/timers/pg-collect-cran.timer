# Schedules pg-collect@cran.service (deploy/quadlet/collectors/pg-collect@.container,
# instantiated with %i=cran, script: scripts/collectors/cran.sh).
# Plain systemd timer, not a quadlet unit -- Podman Quadlet has no .timer
# generator (see qlever-rebuild-index.timer's comment) -- installed as-is
# under /etc/systemd/system/.

[Unit]
Description=Schedule for pg-collect@cran.service

[Timer]
OnCalendar=Mon *-*-* 17:00:00
RandomizedDelaySec=120
Persistent=true
Unit=pg-collect@cran.service

[Install]
WantedBy=timers.target
